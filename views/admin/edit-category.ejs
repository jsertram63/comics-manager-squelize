<%- include('../../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    
</head>

<body>
   <%- include('../../includes/navigation.ejs') %>

    <main>
        <form class="product-form" action="/admin/<% if (editing) { %>edit-category<% } else { %>add-category<% } %>" method="POST">
            <div class="form-control">
                <label for="title">category</label>
                <input type="text" name="name" id=name value="<% if (editing) { %><%= product.title %><% } %>">
            </div>
              <!--
            <div class="form-control">
                <label for="imageUrl">Image URL</label>
                <input type="text" name="imageUrl" id="imageUrl" value="<% if (editing) { %><%= product.imageUrl %><% } %>">
            </div>
            
            <% if (editing) { %>
                <input type="hidden" value="<%= book.id %>" name="bookId">
            <% } %>
            -->
            
            <% if (editing) { %>
                <input type="hidden" value="<%= product.id %>" name="productId">
            <% } %>

            <button class="btn" type="submit"><% if (editing) { %>Update Product<% } else { %>Add Category<% } %></button>
        </form>
    </main>
<%- include('../../includes/end.ejs') %>